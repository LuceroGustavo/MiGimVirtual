# Despliegue en servidor (puntero)

La guía **completa y actualizada** de MiGymVirtual en el VPS Donweb (puerto **8081**, SSH, VNC, DNS, Nginx, `.env.production`, scripts) está aquí:

**[DESPLIEGUE-DONWEB-MIGIMVIRTUAL.md](DESPLIEGUE-DONWEB-MIGIMVIRTUAL.md)**

Usá ese archivo como contexto principal para IA o para operar el servidor.

*(Este archivo existía antes con texto copiado de otro proyecto; se deja como redirección para no romper enlaces viejos.)*
